import React from "react";
import images from "../utils/imageImport";

const Loading = () => {
  return (
    <section>
      <img src={images.signature} alt="Loading Image" />
    </section>
  );
};

export default Loading;
