import React from "react";
import { RoundedContainer } from "../components/index";

const ProjectsSection = () => {
  return (
    <section id="projects-section">
      <RoundedContainer>
        <h2 className="text-color">Projects Section</h2>
      </RoundedContainer>
    </section>
  );
};

export default ProjectsSection;
